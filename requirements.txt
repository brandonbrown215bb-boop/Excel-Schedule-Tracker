# Core application dependencies (cross-platform)
openpyxl>=3.1.0
PyQt5>=5.15.0
requests>=2.28.0
PyYAML>=6.0

# --- Development dependencies (not needed at runtime) ---

# Testing
pytest>=8.0
pytest-cov>=5.0

# Linting & formatting
ruff>=0.8.0

# Type checking
mypy>=1.12
